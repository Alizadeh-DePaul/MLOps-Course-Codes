# Local Streamlit dev-server config. HF Spaces ignores this file and uses
# its own defaults. Tweak the theme freely.
[server]
headless = false
runOnSave = true

[theme]
base = "light"
primaryColor = "#1f77b4"
