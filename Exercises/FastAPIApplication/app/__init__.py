"""FastAPI Application exercise package (SE 489 - MLOps, Week 9 / Week 10).

The FastAPI app object lives in `app.main:app`. That dotted path is what
both the `fastapi dev` CLI and `uvicorn` use to find the application, and
it is also what the container's CMD references.
"""
